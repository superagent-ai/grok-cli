#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run linting
echo "Running linter..."
npm run lint || exit 1

# Run type check
echo "Running type check..."
npm run typecheck || exit 1

# Run tests
echo "Running tests..."
npm test || exit 1

echo "✓ Pre-commit checks passed!"
